<?xml version="1.0"?>
<ruleset name="VK Photos">
	<description>WordPress Coding Standards configuration for vk-photos plugin.</description>

	<!-- What to scan. -->
	<file>.</file>

	<!-- Exclude vendor and other directories. -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/build/*</exclude-pattern>
	<exclude-pattern>*/dist/*</exclude-pattern>
	<exclude-pattern>*/assets/*</exclude-pattern>
	<exclude-pattern>*/css/*</exclude-pattern>
	<exclude-pattern>*/js/*</exclude-pattern>
	<exclude-pattern>*/images/*</exclude-pattern>
	<exclude-pattern>*/templates/*</exclude-pattern>
	<exclude-pattern>*/languages/*</exclude-pattern>
	<exclude-pattern>*.min.js</exclude-pattern>
	<exclude-pattern>*.min.css</exclude-pattern>

	<!-- How to scan. -->
	<arg value="extensions=php"/>
	<arg name="basepath" value="./"/>
	<arg name="colors"/>
	<arg name="warning-severity" value="0"/>
	<arg name="error-severity" value="1"/>

	<!-- Rules: Check PHP version compatibility. -->
	<config name="testVersion" value="8.0-"/>

	<!-- Rules: Set the WordPress standard. -->
	<rule ref="WordPress">
		<!-- Allow short array syntax. -->
		<exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
	</rule>

	<!-- Rules: WordPress-Extra. -->
	<rule ref="WordPress-Extra"/>

	<!-- Rules: WordPress-Docs. -->
	<rule ref="WordPress-Docs">
		<!-- Allow missing @package tag. -->
		<exclude name="WordPress.Commenting.FileHeader.MissingPackageTag"/>
	</rule>
</ruleset>
